we dont want remote requests to run longet than a fixed timeout
Right now the requests in dispatch and requests and
bytes we can haeve objcts as well using that we can deseiralize
before transport.

gossip protocol is defeinetly buggy. if seed stops and comes back alive
it is creating problem. 

we need a way for applciation to decide 
some state that can be synced from the network

we can sync in states like types of adapter available , types of llm models runnong


